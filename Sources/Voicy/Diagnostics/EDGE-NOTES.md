# Edge case audit

- Created EDGE-NOTES.md with required header + Log section.
- Read SelfTest.swift (297 lines): runSelfTestIfRequested(), runSelfTest(), resultLine/resultTier2 helpers, existing checks (Microphone, SpeechRecognition, Contacts, Accessibility, InputMonitoring, WhatsAppInstalled, WhatsAppMedia, TranscriptionEngine).
- Read AliasStore.swift (aliases.json at ~/Library/Application Support/Voicy/aliases.json, JSON dict [String: Entry], Entry is public Codable: spoken/contactIdentifier/e164), Blocklist.swift (blocklist.json at same dir, [String] array, .corrupt state = fail closed), ContactIndex.swift (keys used: identifier, givenName, familyName, nickname, organizationName, phoneNumbers), main.swift (selftest hook already wired), build.sh (dist/Voicy.app signed with stable identity, falls back to ad-hoc; stale-bundle trap documented), Package.swift (platforms: macOS 14).
- Probe 1 (swiftc typecheck) caught two wrong APIs before they hit production code: AVCaptureDevice.inputDevices() does not exist in this SDK (use AVCaptureDevice.default(for: .audio) + DiscoverySession with [.microphone, .external]); SecStaticCodeCreateWithPath takes CFURL, not CFString.
- Probe 2 (runtime): AVAudioEngine.inputNode.inputFormat(forBus: 0) works without triggering the mic TCC prompt (1ch/48kHz here); SecCodeCopySigningInformation returns keys [cdhashes, cdhashes-full, digest-algorithm(s), flags, format, identifier, main-executable, source, unique] with NO TeamIdentifier key; ad-hoc signature = bit 0x2 set in "flags" (observed 131074 = 0x20002 on an ad-hoc binary; kSecCodeSignatureAdhoc constant is not imported to Swift, so use the 0x2 literal per SecCode.h).
- codesign -dv --verbose=2 output shape confirmed: Signature=adhoc / TeamIdentifier=not set for ad-hoc; swiftc now linker-ad-hoc-signs its output, so "unsigned" means SecCodeCopySigningInformation fails.
- This machine HAS a codesigning identity (Apple Development, team YPGV3D5Y8G) - will use it to verify the ad-hoc flag clears for a real Team ID signature.
- Aligned with app conventions: Contact.displayName = given+family | given | organization | "Unknown"; PhoneNormalizer E.164 without '+'; blocklist at ~/Library/Application Support/Voicy/blocklist.json ([String] array, .corrupt = fail closed); aliases at ~/Library/Application Support/Voicy/aliases.json ([String: Entry]).
- CORRECTION (verified on this machine, macOS 26.5.1): AVAudioEngine.inputNode.inputFormat(forBus: 0) SEGFAULTS (exit 139) from the bare binary and from a minimal swiftc probe, with microphone already authorized. Probe 2's "works" claim does not hold for the shipped selftest. checkAudioHardware now reads the default input via CoreAudio property queries (kAudioHardwarePropertyDefaultInputDevice / kAudioDevicePropertyNominalSampleRate / kAudioDevicePropertyStreams + kAudioStreamPropertyVirtualFormat) and AVCaptureDevice.default(for: .audio) for presence; verified 1 ch / 48000 Hz, no prompt, no crash.

